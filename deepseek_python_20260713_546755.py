# syntax_errors.py - A file with multiple syntax errors

def calculate_average(numbers
    # Missing closing parenthesis and colon
    total = 0
    for num in numbers
        # Missing colon after for statement
        total += num
    
    return total / len(numbers)

class Student:
    def __init__(self, name, age, grade
        # Missing closing parenthesis
        self.name = name
        self.age = age
        self.grade = grade
    
    def display_info(self)
        # Missing colon after method definition
        print(f"Name: {self.name}")
        print(f"Age: {self.age}")
        print(f"Grade: {self.grade}"

# Missing closing parenthesis in print statement

if __name__ == "__main__"
    # Missing colon after if statement
    student1 = Student("Alice", 20, "A")
    student1.display_info()
    
    numbers = [10, 20, 30, 40, 50
    # Missing closing bracket in list
    
    avg = calculate_average(numbers
    # Missing closing parenthesis
    
    print("Average is: " avg)
    # Missing comma or concatenation operator
    
    # Invalid indentation
      print("This line has incorrect indentation")
    
    # Using reserved keyword as variable name
    class = "Computer Science"
    
    # Missing quotes around string
    subject = Math
    
    # Incorrect assignment operator
    x == 10
    
    # Missing colon in dictionary
    person = {"name": "John", "age" 30, "city": "NYC"}
    
    # Incorrect string formatting
    message = f"Hello, {name"
    # Missing closing brace
    
    # Unterminated string
    text = "This string is not closed
    
    print("Program completed")